if [ -z $1 ]
then
rental="Unknown"
elif [ -n $1 ]
then
rental=$1
fi
case $rental in
"car") echo "Rs 20" ;;
"van") echo "Rs 10" ;;
"jeep") echo "Rs 5" ;;
"bicycle") echo "20 paisa" ;;
*) echo "Sorry, cannot get $rental for you" ;;
esac
